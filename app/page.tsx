export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", maxWidth: 640 }}>
      <h1>IKI Salesforce Product MCP</h1>
      <p>
        This is a private remote Model Context Protocol server. The MCP endpoint
        is <code>/api/mcp</code> and requires a bearer token. There is nothing to
        see here.
      </p>
    </main>
  );
}
